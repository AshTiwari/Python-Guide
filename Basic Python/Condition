If - else


Syntax:

    >> if <condition> :
    >>      <set of instructions>
    
    >>  elif <condition> :
    >>      <set of instructions>
    
    >> else :
    >>      <set of instructions>
           
