# default dict methods()

from collections import defaultdict

print('It has same functions as a dictionary.')

d = defaultdict(lambda: 'Null')

'''
d.clear
d.copy
d.default_factory
d.fromkeys
d.get
d.items
d.keys
d.pop
d.popitem
d.setdefault
d.update
d.values
'''
